// In a real application, this should be stored securely (environment variable, etc.)
// For this demo, we're using a simple password
export const ADMIN_PASSWORD = 'admin123'; // Change this in production!